import styled from 'styled-components';
import breakpoint from 'styles/breakpoint'

export const StyledDealsPage = styled.div`
  position: relative;
  @media (max-width:${breakpoint.md}){
    margin-top:40px;
  }
`;
